package dsa_11_linked_list;

public class L03_CircularLL {

}

// In circular singly LL, each node keeps track of the next node.
// We use head pointer to keep track of first node of the LL.
// Tail's next node is head.

// In circular doubly LL, each node keeps track of the next node and previous
// node.
// Head's previous node is tail and tail's next node is head.